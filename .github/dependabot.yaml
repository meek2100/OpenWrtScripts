version: 2
updates:
  # Maintain GitHub Actions versions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"

  # Maintain pre-commit hooks (optional, but recommended)
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
